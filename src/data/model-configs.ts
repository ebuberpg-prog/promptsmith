import type { ModelConfig, SupportedModel } from '@/types'

export const MODEL_CONFIGS: Record<SupportedModel, ModelConfig> = {
  midjourney: {
    id: 'midjourney',
    name: 'Midjourney',
    kind: 'midjourney',
    version: 'v8',
    promptStyle: 'midjourney-params',
    supportsNegative: false,
    supportsWeighting: true,
    weightFormat: (tag, weight) => `(${tag}:${weight.toFixed(1)})`,
    triggerWordStyle: 'prefix',
    parameters: ['aspectRatio', 'style', 'chaos', 'quality', 'no'],
  },
  'stable-diffusion': {
    id: 'stable-diffusion',
    name: 'Stable Diffusion 3.5',
    kind: 'tag-based',
    version: '3.5',
    promptStyle: 'comma-separated',
    supportsNegative: true,
    supportsWeighting: true,
    weightFormat: (tag, weight) => `(${tag}:${weight.toFixed(1)})`,
    triggerWordStyle: 'prefix',
    parameters: ['aspectRatio', 'steps', 'cfgScale', 'sampler'],
  },
  'gpt-image': {
    id: 'gpt-image',
    name: 'GPT Image 2',
    kind: 'natural-language',
    version: '2',
    promptStyle: 'structured-prose',
    supportsNegative: false,
    supportsWeighting: false,
    weightFormat: (tag) => tag,
    triggerWordStyle: 'inline',
    parameters: ['aspectRatio', 'quality'],
  },
  flux: {
    id: 'flux',
    name: 'FLUX 2',
    kind: 'tag-based',
    version: '2',
    promptStyle: 'comma-separated',
    supportsNegative: true,
    supportsWeighting: true,
    weightFormat: (tag, weight) => `(${tag}:${weight.toFixed(1)})`,
    triggerWordStyle: 'prefix',
    parameters: ['aspectRatio', 'steps', 'cfgScale'],
  },
  ideogram: {
    id: 'ideogram',
    name: 'Ideogram 3.0',
    kind: 'natural-language',
    version: '3.0',
    promptStyle: 'prose',
    supportsNegative: false,
    supportsWeighting: false,
    weightFormat: (tag) => tag,
    triggerWordStyle: 'inline',
    parameters: ['aspectRatio'],
  },
  'qwen-image': {
    id: 'qwen-image',
    name: 'Qwen Image 2512',
    kind: 'tag-based',
    version: '2512',
    promptStyle: 'comma-separated',
    supportsNegative: false,
    supportsWeighting: true,
    weightFormat: (tag, weight) => `(${tag}:${weight.toFixed(1)})`,
    triggerWordStyle: 'prefix',
    parameters: ['aspectRatio', 'steps'],
  },
  gemini: {
    id: 'gemini',
    name: 'Nano Banana 2',
    kind: 'natural-language',
    version: '2',
    promptStyle: 'prose',
    supportsNegative: false,
    supportsWeighting: false,
    weightFormat: (tag) => tag,
    triggerWordStyle: 'inline',
    parameters: ['aspectRatio'],
  },
  illustrious: {
    id: 'illustrious',
    name: 'Illustrious',
    kind: 'tag-based',
    version: '1.0',
    promptStyle: 'comma-separated',
    supportsNegative: true,
    supportsWeighting: true,
    weightFormat: (tag, weight) => `(${tag}:${weight.toFixed(1)})`,
    triggerWordStyle: 'prefix',
    parameters: ['aspectRatio', 'steps', 'cfgScale', 'sampler'],
  },
  custom: {
    id: 'custom',
    name: 'Custom',
    kind: 'tag-based',
    version: '',
    promptStyle: 'comma-separated',
    supportsNegative: true,
    supportsWeighting: true,
    weightFormat: (tag, weight) => `(${tag}:${weight.toFixed(1)})`,
    triggerWordStyle: 'prefix',
    parameters: ['aspectRatio', 'steps', 'cfgScale', 'sampler'],
  },
}

export const MODEL_GROUPS = [
  {
    label: 'Natural Language',
    models: ['gpt-image', 'gemini', 'ideogram'] as SupportedModel[],
  },
  {
    label: 'Tag-Based',
    models: ['stable-diffusion', 'flux', 'qwen-image', 'illustrious', 'custom'] as SupportedModel[],
  },
  {
    label: 'Midjourney',
    models: ['midjourney'] as SupportedModel[],
  },
]

export function getModelConfig(model: SupportedModel): ModelConfig {
  return MODEL_CONFIGS[model]
}
