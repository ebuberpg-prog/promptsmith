import type { ModelConfig, SupportedModel } from '@/types'

export const MODEL_CONFIGS: Record<SupportedModel, ModelConfig> = {
  midjourney: {
    id: 'midjourney',
    name: 'Midjourney',
    kind: 'midjourney',
    version: 'v6',
    promptStyle: 'midjourney-params',
    supportsNegative: false,
    supportsWeighting: true,
    weightFormat: (tag, weight) => `(${tag}:${weight.toFixed(1)})`,
    triggerWordStyle: 'prefix',
    parameters: ['aspectRatio', 'style', 'chaos', 'quality'],
  },
  'stable-diffusion': {
    id: 'stable-diffusion',
    name: 'SDXL',
    kind: 'tag-based',
    version: '1.0',
    promptStyle: 'comma-separated',
    supportsNegative: true,
    supportsWeighting: true,
    weightFormat: (tag, weight) => `(${tag}:${weight.toFixed(1)})`,
    triggerWordStyle: 'prefix',
    parameters: ['aspectRatio', 'steps', 'cfgScale', 'sampler'],
  },
  'dalle-3': {
    id: 'dalle-3',
    name: 'DALL-E',
    kind: 'natural-language',
    version: '3',
    promptStyle: 'prose',
    supportsNegative: false,
    supportsWeighting: false,
    weightFormat: (tag) => tag,
    triggerWordStyle: 'inline',
    parameters: ['aspectRatio', 'quality'],
  },
  flux: {
    id: 'flux',
    name: 'FLUX',
    kind: 'tag-based',
    version: '1.0',
    promptStyle: 'comma-separated',
    supportsNegative: true,
    supportsWeighting: true,
    weightFormat: (tag, weight) => `(${tag}:${weight.toFixed(1)})`,
    triggerWordStyle: 'prefix',
    parameters: ['aspectRatio', 'steps', 'cfgScale'],
  },
  ideogram: {
    id: 'ideogram',
    name: 'Ideogram',
    kind: 'natural-language',
    version: '2.0',
    promptStyle: 'prose',
    supportsNegative: false,
    supportsWeighting: false,
    weightFormat: (tag) => tag,
    triggerWordStyle: 'inline',
    parameters: ['aspectRatio'],
  },
  'qwen-image': {
    id: 'qwen-image',
    name: 'Qwen Image',
    kind: 'tag-based',
    version: '1.0',
    promptStyle: 'comma-separated',
    supportsNegative: false,
    supportsWeighting: true,
    weightFormat: (tag, weight) => `(${tag}:${weight.toFixed(1)})`,
    triggerWordStyle: 'prefix',
    parameters: ['aspectRatio', 'steps'],
  },
  'z-image': {
    id: 'z-image',
    name: 'z-image',
    kind: 'tag-based',
    version: '1.0',
    promptStyle: 'comma-separated',
    supportsNegative: false,
    supportsWeighting: true,
    weightFormat: (tag, weight) => `[${tag}:${weight.toFixed(1)}]`,
    triggerWordStyle: 'prefix',
    parameters: ['aspectRatio'],
  },
  gemini: {
    id: 'gemini',
    name: 'Gemini',
    kind: 'natural-language',
    version: '1.5',
    promptStyle: 'prose',
    supportsNegative: false,
    supportsWeighting: false,
    weightFormat: (tag) => tag,
    triggerWordStyle: 'inline',
    parameters: ['aspectRatio'],
  },
  custom: {
    id: 'custom',
    name: 'Custom',
    kind: 'tag-based',
    version: '',
    promptStyle: 'comma-separated',
    supportsNegative: true,
    supportsWeighting: true,
    weightFormat: (tag, weight) => `(${tag}:${weight.toFixed(1)})`,
    triggerWordStyle: 'prefix',
    parameters: ['aspectRatio', 'steps', 'cfgScale', 'sampler'],
  },
}

export const MODEL_GROUPS = [
  {
    label: 'Natural Language',
    models: ['dalle-3', 'gemini', 'ideogram'] as SupportedModel[],
  },
  {
    label: 'Tag-Based',
    models: ['stable-diffusion', 'flux', 'qwen-image', 'z-image', 'custom'] as SupportedModel[],
  },
  {
    label: 'Midjourney',
    models: ['midjourney'] as SupportedModel[],
  },
]

export function getModelConfig(model: SupportedModel): ModelConfig {
  return MODEL_CONFIGS[model]
}
